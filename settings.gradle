import org.apache.tools.ant.DirectoryScanner

rootProject.name="vue"

// Include .gitignore and other config files
for(String file in DirectoryScanner.defaultExcludes) {
    DirectoryScanner.removeDefaultExclude(file)
}